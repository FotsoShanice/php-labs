# Google OAuth Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost/job_portal/callback.php

# Database Configuration
DB_HOST=localhost
DB_NAME=job_portal
DB_USER=root
DB_PASS=

# Application Configuration
APP_URL=http://localhost/job_portal
APP_NAME=Job Portal

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual Google OAuth credentials
# 3. Update database settings if different from defaults
# 4. Update APP_URL to match your local setup
# 
# To get Google OAuth credentials:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing one
# 3. Enable Google+ API and Google OAuth2 API
# 4. Go to Credentials → Create Credentials → OAuth 2.0 Client IDs
# 5. Set Application type to "Web application"
# 6. Add authorized redirect URI: http://localhost/job_portal/callback.php
# 7. Copy Client ID and Client Secret to this file
#
# Note: Make sure the redirect URI matches exactly with your local setup
# If your project is in a different folder, update the redirect URI accordingly
